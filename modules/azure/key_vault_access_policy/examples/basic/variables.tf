key_vault_access_policy_01 = {
  # key_vault_id is provided by the root main.
  # tenant_id is provided by the root main.
  # object_id is provided by the root main.
  certificate_permissions = ["Get"]
  secret_permissions      = ["Get"]
}